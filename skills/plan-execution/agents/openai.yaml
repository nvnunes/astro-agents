interface:
  display_name: "Plan Execution"
  short_description: "Execute plans and explicit delegations"
  default_prompt: "Use $plan-execution to carry out the authorized work in the selected plan, including only the delegated Task assignments it explicitly names."
